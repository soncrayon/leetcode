const knapsack = (numItems, capacity) => {
    let maxValue = 0; 
    // iterate over the list of items--most likely an object (weight key, and a value key)
    // object is to add the most value to your knapsack w/o surpassing capacity
    // knapsack itself is a number representing max possible value 
    if (numItems === 0 && capacity === 0) return 0; 
    

    return maxValue; 
}